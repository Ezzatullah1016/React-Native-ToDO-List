# Screenshots (Requirements deliverable)

Add PNG screenshots of the app here for documentation or coursework hand-in.

Suggested captures (after running `npx expo start` in Expo Go or a simulator):

1. `home-tasks.png` — Home tab with at least one task visible.
2. `add-task-modal.png` — New task modal open.
3. `categories.png` — Categories tab.
4. `stats.png` — Stats tab.
5. `settings.png` — Settings tab (theme options visible).
6. `task-detail.png` — Task detail screen (tap a task title on Home).

You can use any file names; link them from the project [README](../README.md) if needed.
